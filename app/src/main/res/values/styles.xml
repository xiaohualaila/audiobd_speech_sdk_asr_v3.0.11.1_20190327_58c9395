<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowFullscreen">true</item>
    </style>
    <style name="dim_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题，设置的是decorview的背景色 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 默认是有阴影背景的,加以下属性可以改变阴影度阴影 -->
    </style>
    <style name="Widget.SeekBar.Normal" parent="@android:style/Widget.SeekBar">
        <item name="android:maxHeight">4.0dip</item>
        <item name="android:indeterminateOnly">false</item>
        <item name="android:indeterminateDrawable">
            @android:drawable/progress_indeterminate_horizontal
        </item>
        <item name="android:progressDrawable">@drawable/seekbar_style</item>
        <item name="android:minHeight">4.0dip</item>
        <item name="android:thumb">@color/title_black</item>
        <item name="android:thumbOffset">10.0dip</item>
    </style>
    <style name="left_photo_score">
        <item name="android:foreground">@drawable/bottom_score_1</item>
        <item name="android:padding">5dp</item>
        <item name="android:scaleType">fitXY</item>
    </style>
    <style name="right_photo_score">
        <item name="android:foreground">@drawable/bottom_score_1</item>
        <item name="android:padding">5dp</item>
        <item name="android:scaleType">fitXY</item>
    </style>


    <style name="bottom_photo_score">
        <item name="android:background">@drawable/bottom_score</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:gravity">center</item>
    </style>
</resources>
